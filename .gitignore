.env.local
.env*.local
.env*.bak*
node_modules/
.next/
.DS_Store
*.tsbuildinfo
next-env.d.ts
.vercel

# runtime dev log
jarvis-dev.log

# Contact-import snapshots — contain PII (emails/phones), regenerable on refresh,
# and the source of truth is Supabase. Keep them local, never in git.
scripts/data/
